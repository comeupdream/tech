# Local STT (speech-to-text) — runs Whisper locally
faster-whisper>=1.0.0

# Local LLM — runs Llama directly in Python, auto-downloads model on first run
llama-cpp-python>=0.3.0
huggingface-hub>=0.24.0

# Local TTS + voice cloning — Resemble AI's Chatterbox (MIT licensed)
chatterbox-tts>=0.1.0

# VAD (voice activity detection)
silero-vad>=5.1

# Audio I/O (microphone + speakers)
sounddevice>=0.5.0
soundfile>=0.12.0

# Audio processing
numpy>=1.26.0
scipy>=1.12.0
torch>=2.1.0
torchaudio>=2.1.0

# Utilities
python-dotenv>=1.0.0

# Scraper (for downloading reference voice clips)
yt-dlp>=2024.1.0
